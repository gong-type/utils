# 永久秒删 v3.0

一个追求**极速**与**强力**平衡的 Windows 永久删除工具。

## ✨ v3.0 极速无感版

解决前代版本启动慢、黑框闪烁的问题，带来极致顺滑体验：

| 特性 | 说明 |
|------|------|
| 🚀 **极速启动** | 采用 VBScript 作为入口，0秒启动，完全无感 |
| 👻 **静默模式** | 普通删除完全无界面、无弹窗、无黑框 |
| 💪 **智能接管** | 仅在遇到顽固文件/占用文件时，自动启动 PowerShell 强力模式 |
| 📦 **批量优化** | 多选批量删除时自动合并请求，消除重复弹窗 |

## 📁 文件结构

| 文件 | 说明 |
|------|------|
| `Wrapper.vbs` | **主入口脚本** (负责快速删除和任务分发) |
| `PermanentDelete.ps1` | **强力模式核心** (负责解锁占用和提权) |
| `install.bat` | 一键安装 |

## 🚀 安装

1. 双击运行 `install.bat`
2. 完成！

## 📖 使用方法

### 右键菜单 (推荐)
- 选中文件/文件夹（支持多选）
- 右键点击 **"永久秒删"**
- 文件瞬间消失（无任何提示）

> 如果文件被占用，会短暂延迟后自动清除，或弹出错误提示。

### 命令行
虽然设计为右键使用，但也可以手动调用：
```powershell
# 强力模式删除 (直接调用内核)
powershell -File C:\Scripts\PermanentDelete.ps1 "path\to\file"
```

## 🔧 架构原理

```
用户点击右键 (多选 N 个文件)
         ↓
    启动 N 个 wscript.exe (极速，无窗口)
         ↓
    Wrapper.vbs写入临时队列
         ↓
    第一个实例启动后台处理器
         ↓
    处理器尝试 VBS 快速删除 (99%情况在此结束)
         ↓ 失败 (被占用/无权限)
    收集失败路径 -> 启动 PowerShell (强力模式)
         ↓
    解锁占用 -> 提权 -> 强制删除
```
